{% raw -%}
[uwsgi]
module={{ctx.project_slug}}.wsgi:application
chdir={{ctx.django_dir}}
pidfile={{ctx.run_dir}}/uwsgi.pid
touch-reload={{ctx.run_dir}}/uwsgi.reload
http-socket={{ctx.run_dir}}/uwsgi.sock
chmod-socket = 666
master=true
vacuum=true
max-requests=50000
processes=4
{%- endraw %}
